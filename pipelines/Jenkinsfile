pipeline {
    agent any

    stages {
        stage('Placeholder') {
            steps {
                echo 'Jenkins pipeline placeholder. GitHub Actions is the active CI/CD pipeline.'
            }
        }
    }
}
